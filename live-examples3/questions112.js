var myFlashcards = [     {question:"processor configured to perform various functions is a means-plus-function limitation",answer:"lakkala"}
     ];
myFlashcards = document.getElementByClass("answer"), i, len = linkArray.length; for (i=0; i<len; i++){ myFlashcards.innerHTML += linkArray[i]; }  

